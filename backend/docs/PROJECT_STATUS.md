# Project Running Status

## Current State

- Backend status: check with the API base URL from your `.env` configuration.
- Frontend status: check with the app URL from your `.env` configuration.
- Database status: verify through the backend health response.

## Notes

- Do not store live service URLs in this document.
- Do not store credentials in this document.
- Keep deployment endpoints in `.env` files only.
